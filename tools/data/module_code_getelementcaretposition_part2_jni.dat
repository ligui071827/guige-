            intObj);

    if (status == IFX_SUCCESS)
    {
        IFX_INT32 returnedInt = (*jniEnv)->CallIntMethod(jniEnv, intObj, HandleCallbacks.GetIntValue);
        *pIndex = returnedInt;
    }

	(*jniEnv)->DeleteLocalRef(jniEnv, intObj);
	
    return status;
}
