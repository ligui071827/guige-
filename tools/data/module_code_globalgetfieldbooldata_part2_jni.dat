            boolObj);

    if (status == IFX_SUCCESS)
    {
        IFX_BOOL returnedBool = (*jniEnv)->CallIntMethod(jniEnv, boolObj, HandleCallbacks.GetIntValueFromBoolean);
        *pData = returnedBool;
    }

    (*jniEnv)->DeleteLocalRef(jniEnv, boolObj);

    return status;
}
